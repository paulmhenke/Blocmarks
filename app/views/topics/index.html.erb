<h1>BlocMarks</h1>
<p>Add a blocmark by sending an email to example@app9e30c2c68f7d4bda946026da2d61c748.mailgun.org</p>

<div class='row'>
  <div class='col-md-10'>
    <% @topics.each do | topic | %>
    <h3><%= topic.title %></h3>
      <% topic.bookmarks.each do | bookmarks | %>
      <li><%= bookmarks.url %></li>
      <% end %>
    <% end %>
  </div>
</div>
